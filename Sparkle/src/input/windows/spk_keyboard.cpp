#include "input/spk_keyboard.hpp"
#include "WinUser.h"

namespace spk
{
	Keyboard::Mapping::Mapping()
	{
		for (int i = 0; i < 255; ++i)
			keys[i] = Key::Unknown;

		bindKey(VK_BACK, Backspace);
		bindKey(VK_TAB, Tab);
		bindKey(VK_CLEAR, Clear);
		bindKey(VK_RETURN, Return);
		bindKey(VK_LSHIFT, LeftShift);
		bindKey(VK_LCONTROL, LeftControl);
		bindKey(VK_LMENU, LeftAlt);
		bindKey(VK_PAUSE, Pause);
		bindKey(VK_CAPITAL, Capslock);
		bindKey(VK_ESCAPE, Escape);
		bindKey(VK_CONVERT, Convert);
		bindKey(VK_NONCONVERT, Non_convert);
		bindKey(VK_ACCEPT, Accept);
		bindKey(VK_MODECHANGE, Mode_change);
		bindKey(VK_SPACE, Space);
		bindKey(VK_PRIOR, Prior);
		bindKey(VK_NEXT, Next);
		bindKey(VK_END, End);
		bindKey(VK_HOME, Home);
		bindKey(VK_LEFT, LeftArrow);
		bindKey(VK_UP, UpArrow);
		bindKey(VK_RIGHT, RightArrow);
		bindKey(VK_DOWN, DownArrow);
		bindKey(VK_SELECT, Select);
		bindKey(VK_PRINT, Print);
		bindKey(VK_EXECUTE, Execute);
		bindKey(VK_SNAPSHOT, Snapshot);
		bindKey(VK_INSERT, Insert);
		bindKey(VK_DELETE, Delete);
		bindKey(VK_HELP, Help);
		bindKey('0', Key0);
		bindKey('1', Key1);
		bindKey('2', Key2);
		bindKey('3', Key3);
		bindKey('4', Key4);
		bindKey('5', Key5);
		bindKey('6', Key6);
		bindKey('7', Key7);
		bindKey('8', Key8);
		bindKey('9', Key9);
		bindKey('A', A);
		bindKey('B', B);
		bindKey('C', C);
		bindKey('D', D);
		bindKey('E', E);
		bindKey('F', F);
		bindKey('G', G);
		bindKey('H', H);
		bindKey('I', I);
		bindKey('J', J);
		bindKey('K', K);
		bindKey('L', L);
		bindKey('M', M);
		bindKey('N', N);
		bindKey('O', O);
		bindKey('P', P);
		bindKey('Q', Q);
		bindKey('R', R);
		bindKey('S', S);
		bindKey('T', T);
		bindKey('U', U);
		bindKey('V', V);
		bindKey('W', W);
		bindKey('X', X);
		bindKey('Y', Y);
		bindKey('Z', Z);
		bindKey(VK_LWIN, LeftWindows);
		bindKey(VK_RWIN, RightWindows);
		bindKey(VK_APPS, Apps);
		bindKey(VK_SLEEP, Sleep);
		bindKey(VK_NUMPAD0, Numpad0);
		bindKey(VK_NUMPAD1, Numpad1);
		bindKey(VK_NUMPAD2, Numpad2);
		bindKey(VK_NUMPAD3, Numpad3);
		bindKey(VK_NUMPAD4, Numpad4);
		bindKey(VK_NUMPAD5, Numpad5);
		bindKey(VK_NUMPAD6, Numpad6);
		bindKey(VK_NUMPAD7, Numpad7);
		bindKey(VK_NUMPAD8, Numpad8);
		bindKey(VK_NUMPAD9, Numpad9);
		bindKey(VK_MULTIPLY, NumpadMultiply);
		bindKey(VK_ADD, NumpadPlus);
		bindKey(VK_SEPARATOR, NumpadSeparator);
		bindKey(VK_SUBTRACT, NumpadMinus);
		bindKey(VK_DECIMAL, NumpadDecimal);
		bindKey(VK_DIVIDE, NumpadDivide);
		bindKey(VK_F1, F1);
		bindKey(VK_F2, F2);
		bindKey(VK_F3, F3);
		bindKey(VK_F4, F4);
		bindKey(VK_F5, F5);
		bindKey(VK_F6, F6);
		bindKey(VK_F7, F7);
		bindKey(VK_F8, F8);
		bindKey(VK_F9, F9);
		bindKey(VK_F10, F10);
		bindKey(VK_F11, F11);
		bindKey(VK_F12, F12);
		bindKey(VK_F13, F13);
		bindKey(VK_F14, F14);
		bindKey(VK_F15, F15);
		bindKey(VK_F16, F16);
		bindKey(VK_F17, F17);
		bindKey(VK_F18, F18);
		bindKey(VK_F19, F19);
		bindKey(VK_F20, F20);
		bindKey(VK_F21, F21);
		bindKey(VK_F22, F22);
		bindKey(VK_F23, F23);
		bindKey(VK_F24, F24);
		bindKey(VK_NUMLOCK, NumLock);
		bindKey(VK_SCROLL, Scroll);
		bindKey(VK_LSHIFT, LeftShift);
		bindKey(VK_RSHIFT, RightShift);
		bindKey(VK_LCONTROL, LeftControl);
		bindKey(VK_RCONTROL, RightControl);
		bindKey(VK_LMENU, LeftMenu);
		bindKey(VK_RMENU, RightMenu);
		bindKey(VK_OEM_1, SemiColon);
		bindKey(VK_OEM_PLUS, Plus);
		bindKey(VK_OEM_COMMA, Comma);
		bindKey(VK_OEM_MINUS, Minus);
		bindKey(VK_OEM_PERIOD, Period);
		bindKey(VK_OEM_2, QuestionMark);
		bindKey(VK_OEM_3, Tilde);
		bindKey(VK_OEM_4, LeftBracket);
		bindKey(VK_OEM_5, VerticalLine);
		bindKey(VK_OEM_6, RightBracket);
		bindKey(VK_OEM_7, Quote);
		bindKey(VK_OEM_8, Unknow);
		bindKey(VK_OEM_102, AngleBracket);
		bindKey(VK_PROCESSKEY, Process);
	}
}