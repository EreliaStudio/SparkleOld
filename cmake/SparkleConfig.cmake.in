@PACKAGE_INIT@

include("${CMAKE_INSTALL_PREFIX}/lib/cmake/@LIBRARY_NAME@/@LIBRARY_NAME@Targets.cmake")

find_package(Vulkan REQUIRED)

set(CMAKE_CXX_STANDARD 20)

set(Sparkle_INCLUDE_DIR "@CMAKE_INSTALL_PREFIX@/includes/Sparkle/" ${Vulkan_INCLUDE_DIRS})
set(Sparkle_LIBRARY_DIR "@CMAKE_INSTALL_PREFIX@/lib/Sparkle/")

if(UNIX)
  set(Sparkle_LIBRARIES "Sparkle")
else()
  set(Sparkle_LIBRARIES "Sparkle.lib")
endif()