@PACKAGE_INIT@

include("${CMAKE_INSTALL_PREFIX}/lib/cmake/@LIBRARY_NAME@/@LIBRARY_NAME@Targets.cmake")

find_package(GLEW REQUIRED)

set(CMAKE_CXX_STANDARD 20)

set(Sparkle_INCLUDE_DIR "@CMAKE_INSTALL_PREFIX@/include")
set(Sparkle_LIBRARY_DIR "@CMAKE_INSTALL_PREFIX@/lib")

if(UNIX)
  set(Sparkle_LIBRARIES "libSparkleOpenGL.a")
else()
  set(Sparkle_LIBRARIES "SparkleOpenGL.lib")
endif()